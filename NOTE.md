# 智能体Agent

## 定义

通过**传感器**（Sensors）**感知**其所处环境（Environment），并**自主**地通过**执行器**（Actuators）采取**行动**以达成特定目标。

关键点在于**自主性**，其赋予智能体“智能”。

### 传统智能体

1. **反射智能体**

   决策核心由固定的“**条件-动作**”规则构成。缺陷在于环境的状态不足以支撑决策，不具备上下文的概念。

2. **基于模型的反射智能体**

   拥有内部的**世界模型**，让模型具备初级的记忆，通过更连贯、更完整的世界状态理解。缺陷在于无法对明确的目标进行主动行动。

3. **基于目标的智能体**

   **主动地**、**有预见地**选择能够导向某个特定**未来状态**的**行动**。 核心能力在于对未来的考量与规划。

4. **基于效用的智能体**

   在达成目标的路径中会存在多种，我们需要满意度最高的路径，以达到**最大化期望效用**。决策上更接近人类的**理性选择**。

5. **学习型智能体**

   **与环境互动自主学习**，而不依赖人类提供的先验知识。其中**强化学习**便是实现这一思想最具代表性的路径。学习型智能体包含性能元件（各类智能体）和学习元件，学习元件通过观察性能元件在环境中的行动所产生的结果来迭代修正性能元件的决策策略。

### LLM新范式

传统智能体的能力来自于工程师的**显式编程**与知识构建，其行为模式是**确定且有边界的**；LLM智能体则是通过在**海量数据**中**预训练**过获得**隐式的世界模型**与强大的涌现能力，能够更加灵活通用地应对复杂任务。

<div align="center">
  <p>表 1.1 传统智能体与 LLM 驱动智能体的核心对比</p>
  <img src="https://raw.githubusercontent.com/datawhalechina/Hello-Agents/main/docs/images/1-figures/1757242319667-2.png" alt="图片描述" width="90%"/>
</div>

LLM智能体工作方式：

- 规划与推理：将目标分解为一系列子任务，由模型驱动整个规划过程。
- 工具使用：智能体在识别到模型的信息缺口，主动调用给定的外部工具进行补全。
- 动态修正：在交互的过程中，用户反馈被智能体视为新的约束，依据此重新调整后续的行动，如此迭代循环最终完成目标。

### 智能体类型

主要从三个视角对智能体进行分类。

#### 基于内部决策架构的分类

从传统智能体的演变过程便构成了分类阶梯，从简单的反应式智能体，到内嵌模型的模型式智能体，再到更具前瞻性的基于目标和基于效用的智能体，最后是学习型智能体通过与环境互动自主学习赋予前面所有模型迭代修正的决策策略。

#### 基于时间与反应性的分类

主要从智能体处理决策的时间维度进行分类，智能体在思考时间与决策质量之间存在非线性关系，不同视角会产生不同的结果，体现在**反应性**和**规划性**之间的平衡。

<div align="center">
  <img src="https://raw.githubusercontent.com/datawhalechina/Hello-Agents/main/docs/images/1-figures/1757242319667-3.png" alt="图片描述" width="90%"/>
  <p>图 1.3 智能体决策时间与质量关系图</p>
</div>

- **反应式智能体**：追求反应性则需要舍弃其长远规划性，只能关注于局部最优，难以适应多步骤协调的复杂任务；

- **规划式智能体**：追求决策质量则需要牺牲反应速度，利用内部的世界模型系统性探索未来状态的各种可能性，基于目标和效用的智能体便是典型的规划式智能体。
- **混合式智能体**：对于一些任务需要即时反应也需要长远规划，结合两者优点实现平衡。现代的LLM智能体中，通过一个“思考-行动-观察”的循环运作，将两种模式融为一体，具体体现在如下两种：
  - **规划**：在智能体推理阶段，LLM分析当前状况，进行理性思考，规划出下一步的合理行动。
  - **反应**：在行动和观察阶段，智能体与外部工具或环境交互，能够获得立即反馈。

#### 基于知识表示的分类

- 符号主义AI

  传统人工智能，其核心理念在于智能源自对**符号**的**逻辑操作**。这里的符号表示人类可读的实体（如语言、文字），严格遵循的操作逻辑规则。由于其透明性和可解释性，推理步骤明确，所以其决策过程可预见、可追溯。

- 亚符号文化AI

  知识内隐分布在一个由大量神经元组成的复杂网络中，其统计模式是从海量数据中学习得到的。典型代表有**神经网络**和**深度学习**。其在模式识别上具备优势。

- 神经符号主义AI

  结合前两者范式的优势，使其能够像神经网络在数据中学习，也能像符号系统一样进行逻辑推理。凭借直觉思维、条理清晰审慎思维融合在一起。

## 智能体的构成与运行原理

### 任务环境定义

依据智能体所处的**任务环境**来理解其运作。其中任务环境使用PEAS模型来精确描述。即分析其性能度量、环境、执行器和传感器。

根据LLM智能体所处的数字环境影响其最终的设计。

1. 环境通常是**部分可观察的**。智能体通过工具调用获取数据，只能获取到部分信息，因为它只限于这些工具调用，它要求智能体必须具备记忆和探索能力，从而获取历史信息和推理预测的能力。
2. 行动的结果会有**不确定性**。因为环境会存在随机性，在两次行动前后会因为环境变化而有所不同，这要求智能体能够具备处理不确定性、监控变化并及时决策的能力。
3. 存在多个行动者形成**多智能体环境**，它们间接影响到智能体所处环境的状态，这要求智能体必须快速响应和选择策略的要求。
4. 几乎所有任务都是发生在**行动影响未来且动态变化**的环境中。“感知-思考-行动-观察”循环必须能够快速、灵活地适应持续变化的世界。

### 运行机制

<div align="center">
  <img src="https://raw.githubusercontent.com/datawhalechina/Hello-Agents/main/docs/images/1-figures/1757242319667-5.png" alt="图片描述" width="90%"/>
  <p>图 1.5 智能体与环境交互的基本循环</p>
</div>

### 感知与行动

规范智能体每一次的输出，通过结构化定义展示其思考过程和最终决策。主要包括两个部分：

- **思考**：内部决策的快照，以自然语言形式阐述智能体内部推理过程，并规划出下一步的具体行动。
- **行动**：基于思考后决定对环境施加的具体操作，通常以函数调用形式表示。

最后通过观察在环境中执行行动后返回的信息经过传感器解析封装成简洁清晰的自然语言文本。其会作为下一轮智能体输入的主要信息。

## 动手实现一个简单智能体

构建一个处理分步任务的智能旅行助手

客户端用于调用LLM服务生成回应，需要系统提示词和用户输入，分别存在两个角色：system、user。

观察结果会记录在提示词记录中，用作下一轮智能体输入，依次循环。

## 智能体应用的协作模式

### 作为高效工具深度融入工作流；

AI编程辅助工具：Github Copilot、Claude Code、Trae、Cursor

### 作为自主的协作者通过多智能体协作完成复杂目标：

将高层级目标委托给智能体，智能体主动追求目标实现。架构范式主流方向有：

1. **单智能体自主循环**：通过“思考-规划-行动-复盘”的**闭环**，不断自我**迭代**，最终完成一个开放式的高层目标。
2. **多智能体协作**：**模拟人类团队的协作模式**来解决复杂问题。细分为不同模式：**角色扮演式对话**（不同角色拥有不同沟通协议，它们在一个结构化的对话中协同完成任务）、**组织化工作流**（模拟一个分工明确的虚拟团队，每个智能体赋予工作流程SOP，通过层级化或顺序化的方式协作，产出高质量的复杂成果）。
3. **高级控制流架构**：智能体构建出更强大的**底层工程基础**。将智能体执行过程建模为状态图，更加灵活可靠地实现循环、分支、回溯以及人工介入复杂流程，例如**LangGraph**等框架。

### Workflow和Agent的差异

#### Workflow：

让AI**按部就班**地执行**指令**。作为传统的自动化范式，其核心在于**对一系列任务或步骤进行预先定义的、结构化的编排**，规定了在何种条件下、以何种顺序执行哪些操作。

#### Agent：

赋予AI自由度去**自主**完成目标。LLM智能体以目标为导向，不仅仅执行预设指令，而且能够理解环境、思考推理、规划、动态行动以此循环最终达成目标。基于**实时信息**进行**动态推理**和**决策**的能力，正是Agent的核心价值所在。

## 习题



# 发展史

<div align="center">
  <img src="https://raw.githubusercontent.com/datawhalechina/Hello-Agents/main/docs/images/2-figures/1757246501849-00.png" alt="图片描述" width="90%"/>
  <p>图 2.1 AI智能体的演进阶梯</p>
</div>

**每一个新范式的出现，都是为了解决上一代范式的核心痛点或根本局限。**

>  围绕智能体演变历史分析新范式解决了上一代范式的什么问题？

从明确的逻辑流程到神经网络深度学习再到自主学习外部环境知识，通过“感知-规划-行动-观察”迭代循环得到最优结果。

# LLM基础

## 语言模型与基础架构

### 统计方法

一个句子出现的概率=该句子中每个词出现的条件概率的连乘

$$P(S)=P(w_1,w_2,…,w_m)=P(w_1)⋅P(w_2∣w_1)⋅P(w_3∣w_1,w_2)⋯P(w_m∣w_1,…,w_{m−1})$$

### 词嵌入

在一个高维语义空间中，计算两个向量之间的语义关系（相似度度量）是通过**余弦相似度**计算得到

### RNN

为网络增加记忆能力，解决了词嵌入无法捕捉长距离依赖的问题，但是其必须按顺序处理数据，无法进行大规模的并行计算。

### Transformer

用注意力机制替代了循环结构，实现了序列数据的并行处理，让模型能够同时看到所有位置之间的关系。

输入的每个词都被映射成向量，其中包括了和其他词之间的权重大小，权重越大代表其与当前词的关联性越强。

每个词元向量和三个不同、可训练的权重矩阵（$W_Q$、$W_K$、$W_V$）进行矩阵相乘得到$Q$、$K$、$V$。所以每个词元都有不同的$Q$、$K$、$V$。

> 这里的权重矩阵$W_Q$、$W_K$、$W_V$是通过模型训练过程中通过反向传播和梯度下降从海量数据中自动学习出来的参数。反向传播是通过输出后的损失值从输出端一层层回传，计算每个W矩阵对误差值的贡献度（梯度），再微调更新参数让误差变小，最终得出最优权重。

可以理解为$Q$是个人标签，爱好的东西，$K$是各种各样的主题标签，点积计算的就是个人对其他标签的爱好程度，如果越匹配则权重值越高。根据计算得出的权重来获取其对不同词元$V$的信息获取权重大小计算，最终求和得到**注意力权重向量**。

由于模型只关注于一种关系，所以引入**多头注意力机制**为模型训练后的权重赋予对不同关系的关注。

**逐位置前馈网络FFN**进行两次线性变换$FFN(x) = ReLU(0, xW_1+b_1)W_2+b_2$，其中ReLU激活函数将负数全部置为0，正数不变：$ReLU(z) = max(0, z)$。其主要作用是为聚合后的信息进行非线性变换，提取更高层次的特征。

> 有些概念是比较基础且重要的，例如**梯度**、**反向传播**这些作为每次训练时的重要概念。

对于词元顺序和位置信息，通过引入**位置编码**来解决。

### Decoder-Only

**语言模型的核心任务在于预测下一个词的出现。**

只使用解码器利用自回归的工作模式来实现词预测。通过**掩码自注意力机制**实现对当前词之前的信息进行关注。

## 与语言模型的交互

概率分布：所有可能发生的结果中，每个结果发生的概率分别为多少。

通过`Temperature`配置模型的采样策略。值越大，概率分布越平坦，低概率词获得更多机会，输出更多样发散；值越小，概率分布越陡峭，高概率词奖励更多，输出更加确定、重复性高。

提示工程

如何将文本提取成Token词元呢？





ReAct模式：通过Observation了解工具执行结果，从而决定下一步做什么，如此迭代循环”Thought-Action-Observation“。
